/* eslint-disable no-unused-vars */

export enum ORDER_STATUS {
  PROCESSED = "Processed",
  SHIPPED = "Shipped",
  DELIVERED = "Delivered",
  CANCELLED = "Cancelled",
}

export enum FETCH_METHOD {
  GET = "GET",
  POST = "POST",
  PATCH = "PATCH",
  DELETE = "DELETE",
}
